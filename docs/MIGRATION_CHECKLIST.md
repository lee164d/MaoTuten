# MIGRATION_CHECKLIST

本文件用于集中记录：**当前已确认、但因平台写回限制尚未完成的旧文件原位修改**。

使用方式：
1. 打开对应文件
2. 搜索下面给出的原文块
3. 用替换后的文本覆盖
4. 保存并提交

---

# Top 1｜统一协作架构表述

目标架构：
- 用户 = 决策者
- 协调者AI = 浏览器自动化协调层
- L / Claude = 策划者AI
- T / GPT = 评估者AI + GitHub执行者
- GE / Gemini = 执行者AI（当前仅负责 NB2 图片生成）

## 1. docs/猫图腾_生产线手册_V1.0.md

### 搜索原文
```md
4.1 协作架构

当前阶段为两方协作：

人类（决策者+操作者）
- 拥有最终决策权和全局直觉
- 负责所有关键节点确认和不可委托的工具操作
- 唯一能看到"全局画面"的角色

AI策划者（Claude对话）
- 拥有项目上下文记忆
- 负责写prompt、判断生成结果（技术层面）、给修正方向
- 不能操作浏览器或工具
- 不了解工具最新能力，不确定时先问再写
- 美感判断归属人类，策划者只做技术判断（一致性/构图/prompt遵从度）

中间层AI（Chrome侧边栏）为可选辅助工具，不是信息流必经节点。
当其图片上传能力或共享存储方案跑通后，可重新评估三方架构。
```

### 替换为
```md
4.1 协作架构

当前阶段采用“用户决策 + 四AI角色分工”的协作架构：

用户（决策者）
- 拥有最终决策权和全局直觉
- 负责所有关键节点确认与最终 Go / No-Go
- 是唯一对“是否成立、是否发布”做最终裁决的角色

协调者AI（浏览器自动化工具）
- 负责跨平台消息传递、结果搬运与流程协调
- 不负责策划判断，不负责独立评估，不负责最终决策
- 是用户、Claude、GPT、Gemini之间的操作协调层

L / Claude（策划者AI）
- 负责调研、拆解、作战卡、prompt、技术审核、复盘
- 不直接承担浏览器自动化操作
- 不替代人类做最终美感判断

T / GPT（评估者AI + GitHub执行者）
- 负责独立质量评估、策划方案交叉验证、框架一致性审计
- 负责仓库文档修订、结构治理与框架级审计
- 不负责图片生成执行

GE / Gemini（执行者AI）
- 当前仅承担图片生成与链式图像编辑执行
- 当前仅使用其内部 NB2 能力生成图片
- 不负责项目策划、仓库治理与最终判断

执行原则：
- 用户负责最终判断
- 协调者AI负责跨平台传递与协调
- Claude 负责策划
- GPT 负责评估与 GitHub 执行
- Gemini 负责图片生成执行
```

## 2. README.md

### 搜索原文
```md
- 用“人类决策 + AI策划 + 工具执行”的方式持续生产与迭代
```

### 替换为
```md
- 用“用户决策 + 协调者AI协调 + Claude策划 + GPT评估/仓库执行 + Gemini图片执行”的方式持续生产与迭代
```

### 搜索原文
```md
- 协作系统、策划者AI、决策者、执行者的角色边界已文档化
```

### 替换为
```md
- 协作系统、协调者AI、Claude策划者AI、GPT评估者AI + GitHub执行者、Gemini执行者AI、用户决策者的角色边界已文档化
```

### 搜索原文
```md
### 3. 人类负责判断，AI负责规模化生产
- **人类**：方向判断、美感裁决、关键节点 Go / No-Go
- **策划者AI**：调研、拆解、作战卡、prompt、技术审核、复盘
- **执行者AI / 工具**：MJ、Gemini、可灵、即梦、SORA、CapCut 等环节执行
```

### 替换为
```md
### 3. 用户负责最终判断，四AI角色负责分工协作
- **用户（决策者）**：方向判断、美感裁决、关键节点 Go / No-Go
- **协调者AI**：跨平台消息传递、结果搬运与流程协调
- **L / Claude（策划者AI）**：调研、拆解、作战卡、prompt、技术审核、复盘
- **T / GPT（评估者AI + GitHub执行者）**：独立质量评估、策划方案交叉验证、框架一致性审计、仓库文档执行
- **GE / Gemini（执行者AI）**：当前仅承担 NB2 图片生成与链式图像编辑执行
```

## 3. docs/AI创意生产协作系统手册_V1.0.md

### 搜索原文
```md
### 2.1 角色定义

系统由三个角色组成。在实际运行中可能由两个实体承担（一个人类+一个AI同时承担策划和执行），也可能由三个独立实体承担。

**决策者（人类）**
- 设定项目方向和目标受众
- 所有美感判断的最终裁决权（好不好看、有没有调性、是否发布）
- 在关键节点做Go/No-Go决策
- 选择工具版本和供应商（原则：优先选择最前沿版本，新项目没有历史包袱）

**策划者（AI）**
- 调研和搜索（市场、竞品、用户情绪、平台趋势）
- 方案生成（角色设计方案、prompt编写、分镜结构、内容策略）
- 技术判断（哪个工具能做什么、prompt怎么写效果最好、成本估算）
- 文档输出和维护
- 认知诚实：输出中必须区分「事实」（已验证的数据或结果）、「判断」（基于证据的推断）、「推测」（缺乏数据支撑的猜测）

**执行者（AI或人类）**
- 在指定工具中执行具体操作（MJ出图、Gemini编辑、SD2/可灵生成视频、CapCut后期）
- 按格式要求回传结果
- 报告工具行为的事实（实际消耗、出错信息、输出质量描述）
```

### 替换为
```md
### 2.1 角色定义

系统当前采用“用户决策 + 四AI角色分工”的协作架构。

**用户（决策者）**
- 设定项目方向和目标受众
- 所有美感判断的最终裁决权（好不好看、有没有调性、是否发布）
- 在关键节点做 Go / No-Go 决策
- 对策划、评估、执行结果做最终采纳或否决

**协调者AI（浏览器自动化工具）**
- 负责跨平台消息传递、结果搬运与流程协调
- 不负责项目策划，不负责独立评估，不负责最终判断
- 是用户、Claude、GPT、Gemini之间的操作协调层

**L / Claude（策划者AI）**
- 调研和搜索（市场、竞品、用户情绪、平台趋势）
- 方案生成（角色设计方案、prompt编写、分镜结构、内容策略）
- 技术判断（哪个工具能做什么、prompt怎么写效果最好、成本估算）
- 文档输出和维护

**T / GPT（评估者AI + GitHub执行者）**
- 对生成结果做独立质量评估
- 对策划者方案做交叉验证
- 对框架级文档做一致性审计
- 执行 GitHub 仓库层面的文档修订与结构治理

**GE / Gemini（执行者AI）**
- 当前仅承担 NB2 图片生成与链式图像编辑执行
- 按要求回传图片结果
- 不承担策划、不承担独立评估、不承担最终判断
```

### 搜索原文
```md
### 2.2 信息流

```
决策者 ←→ 策划者：自然语言对话。决策者提需求/做判断，策划者给方案/给选项。
策划者 → 执行者：结构化指令（prompt文本+工具指定+参数+预期输出格式）。
执行者 → 策划者：结构化回传（输出文件/截图+实际参数+异常报告）。
执行者 → 决策者：当且仅当需要美感判断时直接呈现结果。
```

关键约束：当执行者是独立AI实例（如Gemini）时，信息回传可能受工具限制（如无法上传图片到另一个AI的对话窗口）。此时人类需要充当信息中继。这是架构成本，在设计流程时必须考虑。
```

### 替换为
```md
### 2.2 信息流

```
用户 → 协调者AI：下达项目指令与执行要求。
协调者AI ↔ Claude：传递策划需求、方案与反馈。
协调者AI ↔ GPT：传递评估需求、审计任务与 GitHub 文档修订任务。
协调者AI ↔ Gemini：传递图片生成指令与结果。
Claude → 用户：汇报策划方案、技术判断与推进建议。
GPT → 用户：汇报独立评估结论、交叉验证结论与仓库结构建议。
Gemini → 用户：仅通过协调者AI间接回传图片结果。
```

关键约束：协调者AI负责跨平台中继；Claude 负责策划；GPT 负责评估与 GitHub 执行；Gemini 只负责图片生成执行；最终判断统一由用户做出。
```

## 4. docs/猫图腾_执行者AI执行框架_V1.0.md

### 搜索原文
```md
中间层AI执行框架 V1.0
综合自：人类V0.2执行逻辑 + Claude对话操作手册V1.0 + 三方协作流程框架

一、系统架构
本系统由三个角色构成一条单向信息流水线。每个角色有且只有一种核心职能，不可越界。
人类（决策者） — 拥有最终决策权，时间成本最高。负责创意方向选定、所有关键节点确认（参考源头、作战卡、关键帧、视频素材、成片）、以及所有中间层AI做不到的工具操作。不负责任何可自动化的重复性搬运。
Claude对话（策划者） — 运行在claude.ai主对话窗口，拥有项目全部上下文记忆。负责写prompt、判断生成结果质量、给修正方向、写作战卡和字幕方案和发布文案。它没有浏览器操作能力，看不到中间层AI的屏幕，只能通过上传的截图和文件获取信息。它不了解各工具的最新能力和操作细节，可能给出过时或跳层的方案——中间层AI有责任核实后再执行。
中间层AI（执行者） — 运行在Chrome侧边栏，拥有浏览器操作能力。负责在各生成工具上执行操作、在三方之间搬运信息和截图。不负责内容质量判断，不负责创意决策。碰到能力边界时立即交回人类，不尝试绕过。
```

### 替换为
```md
协调者AI执行框架 V1.0
综合自：用户协调逻辑 + Claude策划流程 + GPT评估/GitHub执行流程 + Gemini图片执行协作约束

一、系统架构
本文件定义的是“协调者AI”的执行框架，而不是 Gemini 图片生成规则。

当前系统由以下角色协作：
- 用户（决策者）
- 协调者AI（浏览器自动化工具）
- L / Claude（策划者AI）
- T / GPT（评估者AI + GitHub执行者）
- GE / Gemini（执行者AI，当前仅用其内部 NB2 能力生成图片）

协调者AI的职责：
- 在用户、Claude、GPT、Gemini之间做跨平台消息传递
- 搬运结果、截图、链接与操作反馈
- 协调多角色之间的信息流
- 不负责项目策划
- 不负责独立评估
- 不负责最终判断
- 不直接承担 NB2 图片生成本体
```

---

# Top 2｜PENDING_EVALUATION.md 设为唯一真源

## 1. docs/猫图腾_生产线手册_V1.0.md

### 搜索原文
```md
7.3 待评估清单（当前）

- 中间层AI的图片上传能力是否已解锁
- 共享存储方案（Google Drive文件夹）是否可替代截图搬运
- 可灵Omni vs 即梦SD2的成本效率对比（需要更多样本）
- 角色V3在不同梗类型下的适配度（温情向梗是否需要调整）
- AI漫剧200亿市场对猫图腾的商业模式启示
```

### 替换为
```md
7.3 待评估清单

当前待评估事项统一维护于仓库根目录 `PENDING_EVALUATION.md`。
本文档不再镜像保存条目，以避免双源冲突。
```

## 2. PENDING_EVALUATION.md

### 搜索原文
```md
本文档仅存放已产生初步认知但未闭环验证的事项。不是框架，不具备约束力。每条视频复盘后逐条处决：升级→写入框架 / 否决→移入已否决区 / 继续观察。新对话或新人加入时先扫描本文档。
```

### 替换为
```md
本文档仅存放已产生初步认知但未闭环验证的事项。不是框架，不具备约束力。
**本文件是当前待评估事项的唯一真源（single source of truth）。**
每条视频复盘后逐条处决：升级→写入框架 / 否决→移入已否决区 / 继续观察。新对话或新人加入时先扫描本文档。
```

### 搜索原文
```md
- **关联文档**：reference/BENCHMARK_ACCOUNTS.md（待创建）
```

### 替换为
```md
- **关联文档**：reference/BENCHMARK_ACCOUNTS.md
```

---

# Top 3｜全仓统一“阶段0 = 情绪方向锁定”

## 1. docs/猫图腾_生产线手册_V1.0.md

### 搜索原文
```md
阶段0 · 选梗
→ 从B站梗指南最新期逐个过
→ AI策划者批量评估，给排序
→ 人类选定
```

### 替换为
```md
阶段0 · 情绪方向锁定
→ 从热梗入口获取当期候选
→ AI策划者结合评论语料提取候选情绪方向
→ 人类确认当期情绪方向
```

### 搜索原文
```md
0. 选定梗
```

### 替换为
```md
0. 确认当期情绪方向
```

## 2. docs/猫图腾_策划者AI执行框架_V1.0.md

### 搜索原文
```md
**阶段0 · 选梗**
- 收到中间层AI上传的梗列表截图
- 按时间倒序逐个过，对每个梗回答：最能满足什么情绪价值？能否被猫IP交付？
- 给出四筛子初步评分
- 输出格式见第五节
```

### 替换为
```md
**阶段0 · 情绪方向锁定**
- 收到候选热梗与评论区语料截图
- 对每个候选入口回答：评论区最强共鸣正在指向什么情绪方向？
- 判断该情绪方向能否被猫IP交付
- 给出四筛子初步评分
- 输出格式见第五节
```

### 搜索原文
```md
**梗评估输出：**
```

### 替换为
```md
**情绪方向评估输出：**
```

### 搜索原文
```md
梗名：
播放量/评论数：
最能满足的情绪价值：
猫IP能否交付（视角+组合）：
四筛子：情绪命中X/高级感X/猫适配X/暗扣X
结论：进入下一步 / 淘汰（一句话理由）
```

### 替换为
```md
入口梗 / 参考入口：
评论区最强情绪方向：
代表性评论原话：
猫IP能否交付（视角+组合）：
四筛子：情绪命中X/高级感X/猫适配X/暗扣X
结论：进入下一步 / 淘汰（一句话理由）
```

## 3. docs/猫图腾_决策者执行框架_V1.0.md

### 搜索原文
```md
**决策1 · 选定梗**
阶段：阶段0
输入：策划者AI给出的梗评估排序（已按四筛子初筛过）
看什么：排在前面的2-3个梗，它们的情绪价值方向是否能让你产生"这个有搞头"的感觉
判断标准：你作为目标受众（或你对目标受众的理解），能不能在3秒内解释清楚这条内容为什么有趣
快速路径：不需要参与每个梗的分析过程。策划者批量过完给你排序，你只看前几个做选择。
```

### 替换为
```md
**决策1 · 确认当期情绪方向**
阶段：阶段0
输入：策划者AI给出的候选情绪方向评估（已按四筛子初筛过）
看什么：排在前面的2-3个情绪方向，哪个最能让你产生“这个有搞头”的感觉
判断标准：你作为目标受众（或你对目标受众的理解），能不能在3秒内解释清楚这条内容为什么值得做
快速路径：不需要参与每个入口梗的分析过程。策划者批量过完给你排序，你只看前几个情绪方向做选择。
```

## 4. docs/猫图腾_执行者AI执行框架_V1.0.md

### 搜索原文
```md
阶段0 · 选梗 — 中间层AI打开B站梗指南最新一期，截图梗列表发给策划者。策划者按时间倒序逐个过，判断哪个梗最能满足情绪价值。人类确认选定的梗。
```

### 替换为
```md
阶段0 · 情绪方向锁定 — 协调者AI采集热梗入口与评论区语料截图发给策划者。策划者判断评论区最强共鸣对应的情绪方向，并评估猫IP能否交付。用户确认当期情绪方向。
```

## 5. docs/AI创意生产协作系统手册_V1.0.md

### 搜索原文
```md
**内容生产阶段**
- 策划者执行：选梗（必须有爆款参考源）→ 写作战卡 → 写prompt → 安排生成
- 执行者执行：在指定工具中生成关键帧/视频/后期
- 决策者判断：每个环节的视觉输出是否达标
- 硬约束：没有爆款参考源头就写作战卡=凭空造概念。
```

### 替换为
```md
**内容生产阶段**
- 策划者执行：情绪方向锁定（必须有评论区共鸣支撑）→ 写作战卡 → 写prompt → 安排生成
- 执行者执行：在指定工具中生成关键帧/视频/后期
- 决策者判断：每个环节的视觉输出是否达标
- 硬约束：没有情绪方向支撑、没有参考源头就写作战卡 = 凭空造概念。
```

### 搜索原文
```md
4. 单条内容的梗/视觉壳确认
```

### 替换为
```md
4. 单条内容的情绪方向/视觉壳确认
```

---

# Top 4｜修复 README 与角色路由

## 1. README.md

### 搜索原文
```md
docs/
├── 猫图腾_生产线手册_V1.0.md
├── 猫图腾_视觉叙事规范_V1.0.md
├── 猫图腾_策划者AI执行框架_V1.0.md
├── 猫图腾_决策者执行框架_V1.0.md
├── 猫图腾_执行者AI执行框架_V1.0.md
└── AI创意生产协作系统手册_V1.0.md

automation/
└── ROADMAP.md

reference/
└── BENCHMARK_ACCOUNTS.md

PENDING_EVALUATION.md
```

### 替换为
```md
docs/
├── READ_THIS_FIRST.md
├── 猫图腾_生产线手册_V1.0.md
├── 猫图腾_视觉叙事规范_V1.0.md
├── 猫图腾_策划者AI执行框架_V1.0.md
├── 猫图腾_评估者AI执行框架_V1.0.md
├── 猫图腾_决策者执行框架_V1.0.md
├── 协调者AI执行框架_V1.0.md
├── 猫图腾_执行者AI执行框架_V1.0.md
└── AI创意生产协作系统手册_V1.0.md

docs/templates/
├── 作战卡模板.md
├── 数据回收模板.md
├── 复盘报告模板.md
├── 四筛子评分细则.md
└── 工具参数与Prompt模板.md

automation/
└── ROADMAP.md

reference/
└── BENCHMARK_ACCOUNTS.md

PENDING_EVALUATION.md
```

### 搜索原文
```md
### 文档入口说明

- **生产线手册**：猫图腾项目的核心操作系统
- **视觉叙事规范**：静态画面 / 关键帧如何通过前景、中景、后景承担情绪功能，详见 [`docs/猫图腾_视觉叙事规范_V1.0.md`](./docs/猫图腾_视觉叙事规范_V1.0.md)
- **协作系统手册**：人类 + AI 创意生产的通用框架
- **策划者AI执行框架**：prompt、判断、输出规范、自我约束
- **决策者执行框架**：人类在关键节点的职责与判断标准
- **执行者AI执行框架**：工具执行、回传、异常处理
- **自动化路线图**：生产线各环节自动化现状与待验证事项
- **对标账号参考库**：各情绪赛道的对标账号池，服务阶段0情绪验证、阶段1候选参考源头筛选、阶段6复盘基准对照，详见 [`reference/BENCHMARK_ACCOUNTS.md`](./reference/BENCHMARK_ACCOUNTS.md)

> **⚠️ 待评估清单**：每次复盘或新AI角色加入项目时，必须先扫描 [PENDING_EVALUATION.md](./PENDING_EVALUATION.md)，理解当前非框架更新级别的重点推进要点。该文档存放已产生初步认知但未完成闭环验证的事项，具体审阅规则见文档内部。
```

### 替换为
```md
### 文档入口说明

- **READ_THIS_FIRST**：新人 / 新AI 的最短阅读路径
- **生产线手册**：猫图腾项目的核心操作系统
- **视觉叙事规范**：静态画面 / 关键帧如何通过前景、中景、后景承担情绪功能，详见 [`docs/猫图腾_视觉叙事规范_V1.0.md`](./docs/猫图腾_视觉叙事规范_V1.0.md)
- **协作系统手册**：当前协作架构与角色分工总入口
- **策划者AI执行框架**：Claude 的职责、输出规范、自我约束
- **评估者AI执行框架**：GPT 的独立质量评估、交叉验证、结构审计职责
- **决策者执行框架**：用户在关键节点的职责与判断标准
- **协调者AI执行框架**：浏览器自动化协调层的中继与流程职责
- **执行者AI执行框架**：Gemini / GE 的图片执行边界
- **模板层**：作战卡、四筛子、数据回收、复盘、工具参数与 Prompt 模板
- **自动化路线图**：生产线各环节自动化现状与待验证事项
- **对标账号参考库**：各情绪赛道的对标账号池，服务阶段0情绪验证、阶段1候选参考源头筛选、阶段6复盘基准对照，详见 [`reference/BENCHMARK_ACCOUNTS.md`](./reference/BENCHMARK_ACCOUNTS.md)

> **⚠️ 待评估清单**：每次复盘或新AI角色加入项目时，必须先扫描 [PENDING_EVALUATION.md](./PENDING_EVALUATION.md)。该文档是当前待评估事项的唯一真源。
```

### 在“文档入口说明”后新增
```md
### 推荐阅读顺序

1. 先读 [`docs/READ_THIS_FIRST.md`](./docs/READ_THIS_FIRST.md)
2. 再读 [`docs/猫图腾_生产线手册_V1.0.md`](./docs/猫图腾_生产线手册_V1.0.md)
3. 再按角色进入对应执行框架
4. 开始工作前先扫描 [`PENDING_EVALUATION.md`](./PENDING_EVALUATION.md)
```

## 2. docs/AI创意生产协作系统手册_V1.0.md

### 搜索原文
```md
## 七、角色入口路由

读完本文档后，根据你的角色阅读对应的专属文档：

**如果你是策划者AI →** 阅读《策划者AI执行框架》。你将了解：能力边界定义、认知诚实协议（事实/判断/推测三级标注）、各阶段的输入输出格式、已知陷阱清单。

**如果你是决策者（人类）→** 阅读《决策者执行框架》。你将了解：6个决策节点的判断标准、给AI下指令的最佳实践、效率原则、已知低效模式。

**如果你是执行者AI →** 阅读《执行者操作手册》（如有）。你将了解：各工具的操作规范、回传格式、异常处理流程。
```

### 替换为
```md
## 七、角色入口路由

读完本文档后，按以下顺序进入：

### 7.1 新人通用路径
1. 先读 `docs/READ_THIS_FIRST.md`
2. 再读《猫图腾_生产线手册_V1.0》
3. 再按角色进入专属执行框架
4. 开始工作前扫描 `PENDING_EVALUATION.md`

### 7.2 角色专属路径

**如果你是协调者AI →** 阅读《协调者AI执行框架》。你将了解：跨平台消息传递、结果搬运、流程协调与中继边界。

**如果你是 L / Claude（策划者AI）→** 阅读《策划者AI执行框架》。你将了解：能力边界定义、认知诚实协议、各阶段输入输出格式、已知陷阱清单。

**如果你是 T / GPT（评估者AI + GitHub执行者）→** 阅读《评估者AI执行框架》。你将了解：一级/二级/三级评估顺序、交叉验证边界、一致性审计要求。

**如果你是用户（决策者）→** 阅读《决策者执行框架》。你将了解：关键决策节点、给 AI 下指令的最佳实践、效率原则、已知低效模式。

**如果你是 GE / Gemini（执行者AI）→** 阅读《猫图腾_执行者AI执行框架》。你将了解：NB2 图片生成执行边界、回传要求、异常处理规则。
```

---

# Top 5｜建立最小模板层

以下文件已在仓库中创建完成：
- `docs/templates/作战卡模板.md`
- `docs/templates/数据回收模板.md`
- `docs/templates/复盘报告模板.md`
- `docs/templates/四筛子评分细则.md`
- `docs/templates/工具参数与Prompt模板.md`

若后续扩展模板字段，请直接在这些文件上迭代，不要再只停留在“主文档里提到但未落地”的状态。

---

# 当前状态总结

已完成并落仓：
- 协调者AI执行框架
- 新人入口文档
- 最小模板层
- 本迁移清单

仍待手动原位修改：
- README.md
- docs/猫图腾_生产线手册_V1.0.md
- docs/AI创意生产协作系统手册_V1.0.md
- docs/猫图腾_执行者AI执行框架_V1.0.md
- docs/猫图腾_策划者AI执行框架_V1.0.md
- docs/猫图腾_决策者执行框架_V1.0.md
- PENDING_EVALUATION.md
